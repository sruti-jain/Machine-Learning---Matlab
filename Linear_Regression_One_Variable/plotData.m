function plotData(x, y)

%PLOTDATA Plots the data points x and y into a new figure 
% CODE HERE =================================================
plot(x,y,'rx','MarkerSize', 10);
xlabel('Population')
ylabel('Revenue')
title('Population and revenue plot')
figure; 
% ============================================================

end
